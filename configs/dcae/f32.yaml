# DC-AE f32c32: 256×256 → 8×8×32 (32× spatial compression)
defaults:
  - default

latent_channels: 32
compression_ratio: 32
# Scaling factor derived from ImageNet pretrained weights
# Computed as 1/std of latent activations on calibration set
scaling_factor: 0.3189
